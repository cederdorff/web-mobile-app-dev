import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
    appId: "dk.eaaa.race",
    appName: "ionic-push-notifications",
    webDir: "build",
    bundledWebRuntime: false
};

export default config;
