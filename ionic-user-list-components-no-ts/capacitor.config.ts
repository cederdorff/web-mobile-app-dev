import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
    appId: "io.ionic.starter",
    appName: "ionic-user-list-no-ts",
    webDir: "build",
    bundledWebRuntime: false
};

export default config;
