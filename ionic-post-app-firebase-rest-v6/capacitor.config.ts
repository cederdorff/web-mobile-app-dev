import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
    appId: "eaaa.race.postapp",
    appName: "Post App",
    webDir: "build",
    bundledWebRuntime: false
};

export default config;
